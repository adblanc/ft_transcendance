<script type="text/html" id="negoTemplate">
			<h1 style="margin-top:2px;" class="justify-center flex flex-row"><b> Pending War With : </b></h1>
			<div>
				<img style="margin-left: auto; margin-right: auto; margin-top:10px" width="100" height="100" class="flex flex-row rounded-full p-2" src="{{img}}" />
			</div>
			<a style="margin-top:10px; text-decoration: underline;" href="{{url}}" class="justify-center flex flex-row mb-4"><b>{{name}}</b></a>
            <form action="">
                <label class="block mb-2" for="war-start">
                    <span class="text-gray-700">Start date : {{war.start}} </span>
                    <input
                        class="form-input mt-1 block w-full"
                        type="datetime-local"
						name="start-date"
						placeholder="Click here to change"
                        id="input-start-date"
					/>
				</label>
				<label class="block mb-2" for="war end">
                    <span class="text-gray-700">End date : {{war.end}} </span>
                    <input
                        class="form-input mt-1 block w-full"
                        type="datetime-local"
						name="end-date"
						placeholder="Click here to change"
                        id="input-end-date"
					/>
				</label>
				<label class="block mb-2" for="guild-start">
                    <span class="text-gray-700">Prize wagered :</span>
                    <input
                        class="form-input mt-1 block"
                        type="text"
						name="war-prize"
						value="{{war.prize}}"
                        id="input-prize"
                    />
				</label>
				<label class="block" for="answer-time">
				<p class="text-gray-700">Time to answer a War Time game call (in minutes) : </p>
				<p style="font-size:10px" class="text-gray-700">The game will be automatically won after that time</p>
				<input
					class="form-input mt-1 block w-full"
					type="text"
					name="answer-time"
					value="{{war.time_to_answer}}"
					id="answer-time"
				/>
				</label>
				<label class="block" for="max-calls">
					<p class="text-gray-700">Maximum unanswered calls tolerable during War Time : </p>
					<p style="font-size:10px" class="text-gray-700">If reached, War Time will be over and the guilty guild will lose 10 war points.</p>
					<input
						class="form-input mt-1 block w-full"
						type="text"
						id="max-calls"
						value="{{war.max_unanswered_calls}}"
						id="max-calls"
					/>
				</label>
				<div id="ranked" style="display:none">
					<button style="margin-left:70px" class="floated mt-6 bg-teal-500 text-white rounded-lg shadow-lg py-1 px-2" id="accept" type="button">Accept</button>
					<button class="floated mt-6 bg-red-400 text-white rounded-lg shadow-lg py-1 px-2" id="reject" type="button">Reject</button>
					<div>
						<button class="btn-nego-disabled mt-4 ml-9 bg-teal-500 text-white rounded-lg shadow-lg py-1 px-2" id="negotiate" type="button">Send back your proposed changes</button>
					</div>
				</div>
				<div id="member" class="mt-4 ml-3" style="display:none">
					<h1><b> ! You need to be an officer or higher to negotiate this war !</b></h1>
				</div>

            </form>
</script>
