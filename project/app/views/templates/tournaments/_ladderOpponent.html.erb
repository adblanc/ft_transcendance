<script type="text/html" id="ladderOpponentTemplate">
<div class="row" >
	<div style="padding:5px">
		<table>
		<tr>
			<td> <img style="float:left" width="80" height="80" class="rounded-full p-2 ml-2" src="{{model.avatar_url}}" /></td>
			<td style="padding:0 20px; text-decoration:underline;"><b class="m-auto ml-1"><a href='/user/{{model.id}}'>{{model.login}}</a></b></td>
			<td style="padding:0 20px;"><b class="m-auto ml-1">Rank : {{model.ladder_rank}}</b></td>
			{{#challengeable}}
			<td>
				<button id="challenge-ladder" class="btn-primary" type="button">Challenge</button>
			</td>
			{{/challengeable}}
			{{^challengeable}}
			<td>
				<button class="btn-disabled" type="button">Challenge</button>
			</td>
			{{#justlost}}
				<tr><h1 style="padding:0 20px; font-size:11px;">You just lost to this player. Challenge someone else before trying again.</tr>
			{{/justlost}}
			{{/challengeable}}
		</tr>
		</table>
		<div class="line flex" style="border: 1px solid; color: #14B8A6; margin:auto;"></div>
	</div>
</div>
</script>