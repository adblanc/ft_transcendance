<script type="text/html" id="matchTemplate">
<div class="player">
	{{^finished}}
		<div class="player-name">{{players.0.name}}</div><div class="score">0</div>
	{{/finished}}
	{{#finished}}
		{{#winner}}
		<div class="player-name winner">{{players.0.name}}</div><div class="score">0</div>
		{{/winner}}
		{{^winner}}
		<div class="player-name loser">{{players.0.name}}</div><div class="score">0</div>
		{{/winner}}
	{{/finished}}
</div>
<div class="player">
	{{^finished}}
		<div class="player-name">{{players.1.name}}</div><div class="score">0</div>
	{{/finished}}
	{{#finished}}
		{{#winner}}
		<div class="player-name winner">{{players.1.name}}</div><div class="score">0</div>
		{{/winner}}
		{{^winner}}
		<div class="player-name loser">{{players.1.name}}</div><div class="score">0</div>
		{{/winner}}
	{{/finished}}
</div>
</script>
