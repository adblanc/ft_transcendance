<script type="text/html" id="join-public-channel-template">
  <div class="flex flex-col items-center justify-center">
	<h4 class="text-xl text-center font-bold text-gray-800 underline mb-4">{{name}}</h4>
	<div class="mb-8 flex flex-row items-center justify-center">
		<img
		class="w-12 h-12 rounded-full"
		src="{{ ownerAvatar }}"
		/>
		<div class="ml-4">
			<span class="text-xl text-gray-900 font-semibold">{{ownerLogin}}</span>
				<div class="text-gray-600 text-xs font-semibold">
					{{ownerRole}}
				</div>
		</div>
	</div>
	{{#membersEmpty}}
	<div class="text-gray-600 font-semibold text-center">{{ownerLogin}} is alone here... 😔</div>
	{{/membersEmpty}}
	{{^membersEmpty}}
	<span class="text-lg text-gray-700 font-semibold">Other members : </span>
	<div class="overflow-y-auto h-64">
		{{#members}}
			<div class="flex flex-row items-center p-4">
				<img
					id="user-profile"
					class="w-16 h-16 rounded-full p-2"
					src="{{ avatar_url }}"
				/>
				<div class="w-48">
					{{login}}
					<div class="text-gray-600 text-xs font-semibold">
					{{roomRole}}
					</div>
				</div>
			</div>
		{{/members}}
	</div>
	{{/membersEmpty}}
    <button type="submit" id="join-public-channel" class="btn-primary my-4">
      Join {{name}}
    </button>
  </div>
</script>
