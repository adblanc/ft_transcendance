<script type="text/html" id="messageTemplate">
{{#sent}}
  <div class="flex flex-row-reverse ml-auto">
	  <%= render "templates/profileIcon" %>
      <div class="ml-3 message--sent">
	  {{content}}
	  </div>
    </div>
{{/sent}}

{{^sent}}
  <div class="flex flex-row mr-auto">
	  <%= render "templates/profileIcon" %>
      <div class="ml-3 message--received">
	  {{content}}
	  </div>
    </div>
{{/sent}}
</script>
